<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.online_shopping.mapper.UserMapper">

    <select id="findBy" resultType="User">
        select id, username, password, sex, tel, email, hobby
        from user
        where username = #{username}
    </select>

    <select id="findByconditon" resultType="User">
        select id, username, password, sex, tel, email, hobby
        from user
        <where>
            <if test="id != null">
                id = #{id}
            </if>
            <if test="username != null">
                username = #{username}
            </if>
        </where>
;
    </select>

</mapper>